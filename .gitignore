# Data files are excluded by default so report data is never committed.
# Only the blank template is tracked, via an explicit force-add (git add -f).
*.pdf
*.odg
*.xlsx
*.csv
*.json

# Local-only project context / deployment notes — kept out of the public repo.
CLAUDE.md
STATUS.md
DEPLOY.md
*.local.md
region_stations.md*

# LibreOffice lock files
.~lock.*#

# Local Claude Code settings (personal)
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/
venv/
